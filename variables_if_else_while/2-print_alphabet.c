#include <stdio.h>
/**
 * main - This is the entry point of the code
 *
 * Return: 0 Successful
 */
int main(void)
{

	for (int i = 0; i < 26; i++)
		putchar('a' + i);
	putchar('\n');

	return (0);
}
